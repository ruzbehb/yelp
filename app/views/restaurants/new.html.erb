<%= form_for @restaurant, html: { multipart: true } do |f| %>

<%= f.label :name %>
<%= f.text_field :name %>

<%= f.label :body, "Description" %>
<%= f.text_field :body %>

<%= f.submit :"Add Restaurant" %>
	
<% end %>
